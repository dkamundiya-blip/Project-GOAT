---
trigger: always_on
---

PROJECT GOAT Testing Standards

- Every new subsystem requires dedicated tests.
- All public APIs require validation tests.
- Deterministic replay must be verified.
- Persistence must be round-trip tested.
- Hashes and IDs must be reproducible.
- Regression coverage must never decrease.
- New code must not break frozen steps.